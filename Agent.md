
# **Agent.md — инструкция для Codex/LLM-агента проекта**

## 🎯 **Назначение агента**

Агент управляет кодовой базой проекта, обеспечивая:

* корректную обработку XRD-спектров от необработанного CSV до полной аналитической таблицы;
* надёжное выполнение всего пайплайна: чтение → предобработка → пики → фитинг → фазовый анализ → FoM → вывод;
* возможность расширять функционал, не ломая существующий интерфейс;
* генерацию чистого, структурированного и воспроизводимого кода.

Агент должен быть консервативным: **не ломать рабочий API, не менять сигнатуры публичных функций, не допускать циклических импортов и не переводить функциональность в тяжёлые, монолитные скрипты.**

---

# 📁 **1. Архитектура проекта**

Проект имеет модульную структуру:

```
spectran_app/
  main.py

  src/
    reading/
      data_from.py

    utils/
      preprocessing.py
      spectrum_math.py
      xrd_geometry.py
      peak_metrics.py
      search_match.py

    fitting/
      generate_spec.py
      fit_model.py

  data/
    example/
    phases/

  docs/
    Agent.md
    XRD_Codex.md
```

Агент ДОЛЖЕН соблюдать эту структуру.
Если нужно добавить новый модуль — добавлять его в `src/utils/` или `src/plotting/`.

---

# ⚙️ **2. Общий XRD-pipeline, который агент должен поддерживать**

Pipeline состоит из следующих этапов:

### **1. Чтение данных (`reading/data_from.py`)**

* загрузка CSV-файла в DataFrame с колонками `["two_theta", "intensity"]`.
* опциональное чтение CIF/CSV фазовых данных.

### **2. Предобработка (`utils/preprocessing.py`)**

Шаги, которые агент должен учитывать:

* обрезка диапазона 2θ;
* сглаживание Savitzky–Golay;
* baseline removal;
* нормализация.

### **3. Математика спектров (`utils/spectrum_math.py`)**

Функции:

* нормализация;
* преобразования в градусы/радианы;
* служебные операции над массивами.

### **4. Геометрия XRD (`utils/xrd_geometry.py`)**

* d-spacing из 2θ;
* уравнение Брэгга;
* размер кристаллитов по Шерреру;
* дополнительные преобразования.

### **5. Поиск пиков (`fitting/generate_spec.py`)**

* формирование `spec`;
* поиск пиков `scipy.signal.find_peaks`;
* инициализация параметров моделей.

### **6. Фитинг (`fitting/fit_model.py`)**

* построение composite модели (`Gaussian`, `Lorentzian`, `Voigt`);
* подбор параметров через `lmfit`;
* таблица пиков.

### **7. Метрики пиков (`utils/peak_metrics.py`)**

* площадь пиков;
* классификация кристаллических/аморфных пиков;
* степень кристалличности CI.

### **8. Фазовый анализ (`utils/search_match.py`)**

* сопоставление пиков с эталонными фазами;
* поиск лучшей фазы;
* расчёт FoM-метрики Search–Match;
* ранжирование фаз.

### **9. Визуализация (опционально)**

* функция для отображения пиков и результата.

### **10. Обвязка командной строки (`main.py`)**

* корректная работа с аргументами;
* удобный пользовательский интерфейс.

---

# 🧠 **3. Правила, которые агент обязан соблюдать**

## 🔒 3.1. Нельзя ломать

* сигнатуры функций в:

  * `generate_spec.py`,
  * `fit_model.py`,
  * `main.py`,
  * `spectrum_from_csv()`;
* структуру папок;
* назначение модуля.

## 🔄 3.2. Можно улучшать, но без разрушений

* рефакторить внутренности функций;
* добавлять новые аргументы **с дефолтами** (не меняющими старое поведение);
* добавлять новые модули в `utils/`;
* улучшать комментарии, docstrings и тестовые блоки.

## 🧩 3.3. Взаимосвязанность модулей

Агент обязан соблюдать зависимость:

```
reading → utils → fitting → (utils) → main
```

Запрещено:

* импортировать `main.py` из модулей;
* делать круговые импорты.

---

# 🚦 **4. Что должен уметь агент**

### ✓ Писать корректный Python 3.10+

Типы, docstring, PEP-8.

### ✓ Понимать весь пайплайн XRD

Агент должен уметь модифицировать или объяснять любой участок.

### ✓ Поддерживать Search–Match FoM

Реализация уже есть в `search_match.py`. Агент должен:

* уметь интегрировать FoM в pipeline;
* добавлять фазы;
* оптимизировать сопоставление.

### ✓ Работать с CIF/CSV фазами

Через pymatgen или собственную логику из CSV.

### ✓ Добавлять фичи, если пользователь обратился:

Например:

* сравнение нескольких спектров;
* автоматический экспорт в md/csv;
* построение отчёта для диссертации.

### ✓ Понимать границы ответственности

Если вопрос относится к физике XRD — агент отвечает.
Если нужна новая функция — агент создаёт её правильно в нужном модуле.

---

# 📌 **5. Примерная схема работы, если пользователь загружает XRD.csv**

Агент должен выполнять:

```
df_raw = spectrum_from_csv("file.csv")
df = preprocess(df_raw)
spec = default_spec(df)
spec, peaks_found = update_spec_from_peaks(spec)
model = Model(spec)
output = model.fit()
peak_table = model.print_best_values()
phase_results = search_match_all_phases(peak_table, phase_db)
FoM_list = compute_fom(...)
CI = compute_ci(...)
```

И всё это — без ручного вмешательства.

---

# 🧭 **6. Когда агент может менять код**

Если пользователь пишет:

* *«Добавь новый параметр»*
  → агент добавляет аргумент с безопасным дефолтом.

* *«Ускорь поиск пиков»*
  → агент обновляет алгоритм в `generate_spec.py`.

* *«Улучши baseline»*
  → агент меняет `utils.preprocessing`.

* *«Сделай SearchMatch более точным»*
  → агент улучшает `search_match.py`.

Агент обязан отчитываться, какие файлы он изменил и почему.

---

# 📄 **7. Формат патчей**

Любая генерация кода должна:

* указывать файл,
* указывать, что добавлено/исправлено,
* НЕ генерировать весь проект целиком, если нужно изменить только часть.

---

# 🎓 **8. Взаимодействие с диссертацией**

Агент должен:

* уметь описать любой этап XRD-pipeline научным языком;
* учитывать, что код будет использован как методология в работе;
* при необходимости — готовить текстовые фрагменты для вставки в главы.

---

# 🟢 **9. Резюме**

Агент — это «мозг» проекта.
Его ключевые задачи:

* быть аккуратным, консервативным и последовательным;
* понимать весь pipeline от CSV до FoM;
* не ломать интерфейсы;
* улучшать код, когда пользователь просит;
* поддерживать совместимость и расширяемость.

🚫 10. Запрет на дублирование функций и логики

Агент НЕ ДОЛЖЕН создавать новые функции, которые дублируют уже существующую логику (по смыслу или по названию), за исключением явного запроса пользователя.

10.1. Обязательные шаги перед написанием новой функции

Перед тем как добавить новую функцию/класс/модуль, агент обязан:

Просканировать существующий код:

искать по названию и по ключевым словам;

пример: перед созданием compute_fom, preprocess_xrd, normalize_data, get_d_hkl, scherrer_size — сначала проверить, нет ли таких или аналогичных функций в:

src/utils/,

src/fitting/,

src/reading/.

Если похожая функция уже есть:

использовать её, а не писать заново;

если не хватает аргумента/поведения — расширить существующую функцию:

добавить параметр с безопасным значением по умолчанию;

добавить новую ветку логики с сохранением старого поведения.

Если очень нужна «новая версия»:

дать функции осмысленное другое имя (например, compute_fom_weighted, preprocess_xrd_simple),

в docstring явно указать, чем она отличается от существующей.

10.2. Что делать, если обнаружены дубли

Если агент видит, что:

уже существует несколько версий одной и той же логики (например, две реализации FoM, две normalize-функции, два разных preprocess_xrd),

он должен:

Выбрать одну «каноническую» реализацию (ту, которая:

используется больше,

чище по коду,

лучше документирована).

В остальных местах:

либо удалить дублирующую функцию,

либо переписать её так, чтобы она просто вызывала каноническую (обёртка).

Аккуратно обновить импорты, чтобы весь проект пользовался одной реализацией.

Пример:
если есть:

utils/spectrum_math.normalize_data

и новая utils/preprocessing.normalize_intensity

то агент должен:

оставить одну реализацию (например, в spectrum_math.normalize_data);

сделать в preprocessing обёртку:

from src.utils.spectrum_math import normalize_data as _normalize_data

def normalize_intensity(y):
    """Wrapper around normalize_data for backward compatibility."""
    return _normalize_data(y)


а не копировать код.

10.3. Запрещённые действия

Агент НЕ ДОЛЖЕН:

писать «такую же» функцию в другом модуле только потому, что «удобно здесь»;

переименовывать существующие функции без крайней необходимости;

создавать функции с теми же названиями, но немного другой сигнатурой, ломая старое использование.
